<%= button_to activity_favorite_path(activity, Favorite.find_by(activity_id: activity.id, user_id: current_user.id)), remote: true, method: :delete, class: 'btn-red' do %>
  <%= image_tag "red-fave.svg", alt: "unfave", width: 36, height: 36 %>
<% end %>
